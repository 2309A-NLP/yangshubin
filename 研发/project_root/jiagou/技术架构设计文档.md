# 基于RAG的金融领域智能对话系统 - 技术架构设计文档

---

## 1. 项目概述

### 1.1 项目定位
本系统是一个基于 **Retrieval-Augmented Generation (RAG)** 技术的**金融领域智能对话系统**，支持多角色扮演、多模态输入（图片/语音/视频）、长期记忆和流式输出。

### 1.2 核心技术特性

| 特性 | 说明 |
|------|------|
| **多角色支持** | 11种内置角色（金融理财师、投资顾问、医生、律师等） |
| **RAG增强** | 混合检索（稠密向量+BM25稀疏向量）+ BGE-Reranker重排序 |
| **长期记忆** | Milvus向量数据库存储用户对话历史 |
| **短期记忆** | Redis缓存最近对话上下文 |
| **多模态输入** | 支持TXT/PDF/DOCX/图片OCR/语音ASR/视频帧提取OCR |
| **流式输出** | SSE流式响应，实时显示AI回答 |
| **多LLM支持** | DeepSeek/百度文心一言/阿里通义千问 |

---

## 2. 系统架构图

### 2.1 整体架构

```mermaid
graph TB
    subgraph 前端层["前端层 (FastAPI HTML)"]
        A["web_frontend_show.py<br/>展示逻辑"]
        B["web_frontend_style.py<br/>样式定义"]
        A --> B
    end

    subgraph 后端层["后端服务层 (FastAPI)"]
        C["main.py<br/>服务入口"]
        D["api.py<br/>API路由与业务逻辑"]
        E["database.py<br/>数据库操作"]
        F["utils.py<br/>工具函数与配置"]
    end

    subgraph 数据存储层["数据存储层"]
        G["MySQL<br/>用户/角色/知识"]
        H["Milvus<br/>向量/知识库/记忆"]
        I["Redis<br/>短期记忆/缓存"]
    end

    subgraph 外部服务["外部服务"]
        J["DeepSeek API<br/>大模型"]
        K["东方财富<br/>股票数据"]
    end

    A --> D
    C --> D
    C --> E
    C --> F
    D --> E
    D --> F
    D --> J
    E --> F
    E --> G
    E --> H
    E --> I
    F --> J
    E --> K
```

### 2.2 技术栈

| 层次 | 技术 | 版本 |
|------|------|------|
| 前端 | FastAPI + HTML/CSS/JS | 内嵌模板 |
| 后端 | FastAPI + Uvicorn | 0.104+ |
| 大模型 | DeepSeek API | v1 |
| 向量化 | BGE-m3 | 1.2+ |
| 重排序 | BGE-rerank | 1.2+ |
| 向量数据库 | Milvus | 2.6+ |
| 关系数据库 | MySQL | 5.7+ |
| 缓存 | Redis | 6.0+ |

---

## 3. 项目文件结构

```
project_root/
│
├── last_duan/                          # 【后端核心目录】
│   ├── __init__.py                     # 包初始化
│   ├── main.py                         # ⭐ 入口文件：服务初始化与启动
│   ├── api.py                          # ⭐ API路由：所有接口定义与业务逻辑
│   ├── database.py                     # ⭐ 数据库层：MySQL/Milvus/Redis操作
│   ├── utils.py                        # ⭐ 工具层：配置/模型/工具函数
│   └── rag_system.log                  # 日志文件
│
├── web_frontend/                       # 【前端目录】
│   ├── web_frontend_show.py            # ⭐ 前端展示逻辑：HTML/JS
│   └── web_frontend_style.py           # ⭐ 前端样式：CSS/主题配置
│
├── peizhi_huanjing/                    # 【配置目录】
│   ├── .env                            # 环境变量配置
│   └── requirements.txt                # 依赖列表
│
├── data/                               # 【数据目录】
│   ├── financial_advisor_qa.csv        # 金融知识数据
│   ├── eastmoney_stock_list.json       # 股票列表缓存
│   └── ...
│
├── jiagou/                             # 【架构文档目录】
│   └── 技术架构设计文档.md              # 本文档
│
└── reload_knowledge.py                 # 知识库加载脚本
```

---

## 4. 核心模块详解

### 4.1 main.py - 服务入口模块

**职责**：服务初始化、应用创建、启动Uvicorn

```mermaid
graph TD
    A["main() 主入口"] --> B["init_services() 初始化服务"]
    A --> C["create_app() 创建应用"]
    A --> D["uvicorn.run() 启动服务"]

    B --> B1["init_database() 初始化MySQL"]
    B --> B2["init_milvus() 初始化Milvus"]
    B --> B3["seed_role_knowledge() 种子知识库"]
    B --> B4["get_embedding_model() 加载向量化模型"]
    B --> B5["get_reranker_model() 加载重排序模型"]
    B --> B6["get_redis_client() 加载Redis"]

    C --> C1["FastAPI() 创建实例"]
    C --> C2["CORSMiddleware 添加CORS"]
    C --> C3["GZipMiddleware 添加压缩"]
    C --> C4["register_api_routes() 注册路由"]
```

**核心函数**：

| 函数名 | 功能 | 输出 |
|--------|------|------|
| `main()` | 主入口，协调初始化和启动 | 启动Uvicorn服务 |
| `init_services()` | 初始化所有依赖服务 | 初始化MySQL/Milvus/Redis/模型 |
| `create_app()` | 创建FastAPI应用实例 | FastAPI实例 |

---

### 4.2 api.py - API路由与业务逻辑模块

**职责**：所有路由接口、请求处理、业务逻辑

```mermaid
graph LR
    subgraph 接口层
        R1["/register 注册"]
        R2["/login 登录"]
        R3["/roles 角色管理"]
        R4["/api/chat_stream 流式对话"]
        R5["/chat 非流式对话"]
        R6["/upload_document 文档上传"]
        R7["/sync_stock_data 股票同步"]
        R8["/health 健康检查"]
    end

    subgraph 业务处理
        P1["文件解析 parse_file_content()"]
        P2["Query改写 rewrite_query()"]
        P3["多源并行检索 ThreadPoolExecutor"]
        P4["结果重排序 rerank_documents()"]
        P5["上下文构建 build_llm_messages()"]
        P6["大模型调用 generate_response()"]
        P7["记忆存储 store_long_term_memory()"]
    end

    subgraph 文件解析
        F1["_perform_ocr() 图片OCR"]
        F2["_perform_speech_recognition() 语音ASR"]
        F3["_process_video() 视频帧提取"]
    end

    R4 --> P1
    P1 --> F1
    P1 --> F2
    P1 --> F3
    P1 --> P2
    P2 --> P3
    P3 --> P4
    P4 --> P5
    P5 --> P6
    P6 --> P7
```

**核心接口**：

| 接口 | 方法 | 功能 |
|------|------|------|
| `/register` | POST | 用户注册 |
| `/login` | POST | 用户登录 |
| `/roles` | GET | 获取角色列表 |
| `/api/chat_stream` | POST | ⭐流式对话（核心） |
| `/chat` | POST | 非流式对话 |
| `/upload_document` | POST | 上传知识文档 |
| `/sync_stock_data` | POST | 同步股票数据 |
| `/health` | GET | 健康检查 |

**核心函数**：

| 函数名 | 功能 |
|--------|------|
| `register_api_routes()` | 注册所有API路由 |
| `build_llm_messages()` | 构建发送给LLM的消息列表 |
| `generate_response()` | 生成大模型回答（流式/非流式） |
| `parse_file_content()` | 解析上传文件（支持多种格式） |
| `_perform_ocr()` | 图片OCR识别 |
| `_perform_speech_recognition()` | 语音识别 |
| `_process_video()` | 视频帧提取+OCR |

---

### 4.3 database.py - 数据库操作模块

**职责**：MySQL/Milvus/Redis的所有数据库操作

```mermaid
graph TD
    subgraph MySQL操作
        M1["用户管理"]
        M2["角色管理"]
        M3["角色知识库"]
        M4["文档元信息"]
    end

    subgraph Milvus操作
        V1["混合检索 hybrid_search()"]
        V2["重排序 rerank_documents()"]
        V3["长期记忆 retrieve/store_long_term_memory()"]
        V4["知识库 add_documents_to_milvus()"]
        V5["股票数据 retrieve_stock_data()"]
    end

    subgraph Redis操作
        R1["get_chat_history()"]
        R2["add_to_history()"]
        R3["clear_history()"]
    end
```

**核心函数**：

| 函数名 | 功能 | 存储 |
|--------|------|------|
| `init_database()` | 初始化MySQL表结构 | MySQL |
| `register_user()` | 用户注册 | MySQL |
| `login_user()` | 用户登录 | MySQL |
| `get_roles()` | 获取角色列表 | MySQL |
| `get_role_prompt()` | 获取角色提示词 | MySQL |
| `hybrid_search()` | ⭐混合检索（稠密+稀疏向量） | Milvus |
| `rerank_documents()` | BGE-Reranker重排序 | - |
| `retrieve_long_term_memory()` | 检索长期记忆 | Milvus |
| `store_long_term_memory()` | 存储长期记忆 | Milvus |
| `add_documents_to_milvus()` | 添加文档到知识库 | Milvus |
| `retrieve_stock_data()` | 检索股票数据 | Milvus |
| `get_chat_history()` | 获取聊天历史 | Redis |
| `add_to_history()` | 添加到聊天历史 | Redis |

---

### 4.4 utils.py - 工具函数与配置模块

**职责**：配置常量、模型管理、工具函数

```mermaid
graph LR
    subgraph 配置常量
        C1["LLM_PROVIDER 大模型配置"]
        C2["MySQL_CONFIG 数据库配置"]
        C3["Redis_CONFIG Redis配置"]
        C4["Milvus配置"]
        C5["RAG参数配置"]
    end

    subgraph 模型管理
        M1["get_embedding_model() BGE-m3"]
        M2["get_reranker_model() BGE-rerank"]
        M3["get_llm_client() LLM客户端"]
    end

    subgraph 工具函数
        T1["hash_password() 密码加密"]
        T2["rewrite_query() Query改写"]
        T3["normalize_answer_text() 文本规范化"]
        T4["chunk_text() 文本分块"]
        T5["simple_chinese_tokenizer() 中文分词"]
    end

    subgraph 角色系统
        R1["ROLE_PROMPT_TEMPLATES 提示词模板"]
        R2["ROLE_DISPLAY_NAMES 显示名称"]
        R3["ROLE_SQLITE_KNOWLEDGE 角色知识"]
    end
```

**核心配置**：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `LLM_PROVIDER` | 大模型提供商 | deepseek |
| `EMBEDDING_DIM` | 向量维度 | 1024 |
| `CHUNK_SIZE` | 文本分块大小 | 512 |
| `TOP_K_RETRIEVE` | 检索Top-K | 5 |
| `TOP_K_RERANK` | 重排序Top-K | 2 |
| `MEMORY_LENGTH` | 记忆长度 | 3 |

**核心函数**：

| 函数名 | 功能 |
|--------|------|
| `get_embedding_model()` | 获取BGE-m3向量化模型 |
| `get_reranker_model()` | 获取BGE-reranker重排序模型 |
| `get_llm_client()` | 获取LLM客户端（支持多提供商） |
| `rewrite_query()` | 基于历史改写用户Query |
| `normalize_answer_text()` | 规范化回答文本 |
| `hash_password()` | 密码哈希加密 |

---

### 4.5 web_frontend_show.py - 前端展示逻辑模块

**职责**：提供登录/注册/角色选择/聊天的Web界面

```mermaid
graph TD
    subgraph 页面组件
        P1["登录/注册表单"]
        P2["角色选择卡片"]
        P3["聊天区域"]
        P4["文件上传"]
        P5["快捷提问面板"]
        P6["表情选择面板"]
    end

    subgraph JavaScript功能
        J1["流式聊天渲染"]
        J2["文件处理与预览"]
        J3["角色切换"]
        J4["历史加载/清空"]
        J5["后端API代理"]
    end

    subgraph 代理接口
        A1["/api/register 转发"]
        A2["/api/login 转发"]
        A3["/api/roles 转发"]
        A4["/api/chat_stream 转发"]
        A5["/health 转发"]
    end

    P1 --> J1
    P3 --> J2
    J1 --> A1
    J1 --> A2
    J1 --> A3
    J1 --> A4
    J1 --> A5
```

**核心功能**：

| 功能 | 说明 |
|------|------|
| 登录/注册 | 用户认证界面 |
| 角色选择 | 11种角色卡片展示 |
| 流式聊天 | SSE实时显示AI回答 |
| 文件上传 | 支持图片/语音/视频 |
| 快捷提问 | 预设问题快速提问 |
| 表情选择 | Emoji表情插入 |
| API代理 | 解决浏览器跨域问题 |

---

### 4.6 web_frontend_style.py - 前端样式模块

**职责**：CSS样式定义、主题配置

```mermaid
graph LR
    subgraph 样式配置
        S1["THEME_CONFIG 主题变量"]
        S2["CSS_STYLES 完整CSS"]
    end

    subgraph 主题变量
        V1["主色调 colors"]
        V2["文字颜色 text"]
        V3["阴影效果 shadows"]
        V4["圆角半径 radii"]
        V5["过渡动画 transitions"]
    end

    subgraph CSS覆盖
        C1["登录表单样式"]
        C2["角色卡片样式"]
        C3["聊天消息样式"]
        C4["按钮样式"]
        C5["暗色主题支持"]
    end

    S1 --> S2
    V1 --> S2
    V2 --> S2
    V3 --> S2
    V4 --> S2
    V5 --> S2
    S2 --> C1
    S2 --> C2
    S2 --> C3
    S2 --> C4
    S2 --> C5
```

---

## 5. 系统执行流程

### 5.1 服务启动流程

```mermaid
sequenceDiagram
    participant User as 用户
    participant Main as main.py
    participant DB as database.py
    participant Utils as utils.py
    participant MySQL as MySQL
    participant Milvus as Milvus
    participant Redis as Redis
    participant LLM as BGE模型

    User->>Main: python main.py

    rect rgb(200, 220, 240)
        Note over Main,LLM: init_services() 初始化阶段
        Main->>DB: init_database()
        DB->>MySQL: 创建连接
        MySQL-->>DB: 连接成功
        DB-->>Main: ✅ MySQL初始化完成

        Main->>DB: init_milvus()
        DB->>Milvus: 建立连接
        Milvus-->>DB: 连接成功
        DB->>Milvus: 创建集合
        Milvus-->>DB: 集合创建完成
        DB-->>Main: ✅ Milvus初始化完成

        Main->>Utils: get_embedding_model()
        Utils->>LLM: 加载BGE-m3
        LLM-->>Utils: 模型加载完成
        Utils-->>Main: ✅ 向量化模型加载成功

        Main->>Utils: get_reranker_model()
        Utils-->>Main: ✅ 重排序模型加载成功

        Main->>Utils: get_redis_client()
        Utils->>Redis: 建立连接
        Redis-->>Utils: 连接成功
        Utils-->>Main: ✅ Redis加载成功
    end

    Main->>Main: create_app()
    Main->>Main: uvicorn.run()
    Main-->>User: ✅ 服务启动完成
```

### 5.2 对话请求流程（核心RAG流程）

```mermaid
flowchart TD
    A[用户发送消息] --> B{有上传文件?}
    B -->|是| C[parse_file_content 解析文件]
    B -->|否| D[继续]
    C --> C1{文件类型判断}
    C1 -->|图片| C2[_perform_ocr 图片OCR]
    C1 -->|语音| C3[_perform_speech_recognition 语音识别]
    C1 -->|视频| C4[_process_video 视频帧提取+OCR]
    C1 -->|文档| C5[PyMuPDF/DOCX解析]
    C2 --> E
    C3 --> E
    C4 --> E
    C5 --> E

    D --> E[get_chat_history 获取短期记忆]
    E --> F[rewrite_query Query改写]
    F --> G[并行四路检索]

    G --> H1[长期记忆检索<br/>retrieve_long_term_memory]
    G --> H2[SQLite角色知识库<br/>retrieve_sqlite_role_knowledge]
    G --> H3[Milvus混合检索<br/>hybrid_search]
    G --> H4[股票数据检索<br/>retrieve_stock_data]

    H1 --> I[结果聚合]
    H2 --> I
    H3 --> I
    H4 --> I

    I --> J[rerank_documents BGE-Reranker重排序]
    J --> K[构建上下文 build_llm_messages]

    K --> L[generate_response 调用大模型]
    L --> M{流式请求?}
    M -->|是| N[流式返回SSE]
    M -->|否| O[返回完整JSON]

    N --> P[边返回边存储]
    O --> P

    P --> Q1[add_to_history 短期记忆<br/>Redis]
    P --> Q2[store_long_term_memory 长期记忆<br/>Milvus]
    P --> Q3[缓存回答 _answer_cache]
    Q1 --> R[返回响应]
    Q2 --> R
    Q3 --> R
```

### 5.3 文件解析流程

```mermaid
flowchart TD
    A[上传文件] --> B{文件类型}

    B -->|TXT/MD| C[直接读取文本]
    B -->|PDF| D[PyMuPDF解析]
    B -->|DOCX| E[python-docx解析]
    B -->|图片| F{OCR识别}
    B -->|语音| G{ASR识别}
    B -->|视频| H{视频处理}

    F -->|Tesseract可用| F1[Tesseract OCR]
    F -->|EasyOCR备用| F2[EasyOCR]
    F -->|均不可用| F3[返回提示]

    G -->|SpeechRecognition| G1[Google/Sphinx ASR]
    G -->|Whisper备用| G2[Whisper ASR]
    G -->|均不可用| G3[返回提示]

    H -->|OpenCV可用| H1[cv2提取帧]
    H1 --> H2[逐帧OCR]
    H -->|OpenCV不可用| H3[返回提示]

    C --> I[文本分块]
    D --> I
    E --> I
    F1 --> I
    F2 --> I
    G1 --> I
    G2 --> I
    H2 --> I

    I --> J[向量化为Context]
```

---

## 6. 数据流与存储

### 6.1 数据库架构

```mermaid
erDiagram
    users {
        bigint id PK
        varchar username UK
        varchar password_hash
        timestamp created_at
        varchar email
        varchar phone
    }

    roles {
        bigint id PK
        varchar role_name UK
        varchar display_name
        text description
        text personality
        text tone
        text background_story
        text hobbies
        text prompt_template
        bigint creator_user_id FK
        timestamp created_at
        timestamp updated_at
    }

    knowledge_docs {
        bigint id PK
        varchar doc_id UK
        varchar title
        varchar source
        text content
        int chunk_count
        timestamp created_at
        timestamp updated_at
    }

    role_knowledge {
        bigint id PK
        bigint role_id FK
        varchar title
        text content
    }

    users ||--o{ roles : "creates"
    roles ||--o{ role_knowledge : "has"
```

### 6.2 Milvus集合设计

```mermaid
graph LR
    subgraph 知识库集合["知识库集合 financial_knowledge_role_{id}"]
        K1["id: INT64 主键"]
        K2["text: VARCHAR 文本"]
        K3["dense_vector: FLOAT_VECTOR 稠密向量"]
        K4["sparse_vector: SPARSE_FLOAT_VECTOR 稀疏向量"]
        K5["source: VARCHAR 来源"]
        K6["create_time: INT64 创建时间"]
        K7["summary: VARCHAR 摘要"]
        K8["doc_id: VARCHAR 文档ID"]
    end

    subgraph 长期记忆集合["long_term_memory"]
        M1["id: INT64 主键"]
        M2["user_id: INT64 用户ID"]
        M3["role_id: INT64 角色ID"]
        M4["memory_text: VARCHAR 对话内容"]
        M5["dense_vector: FLOAT_VECTOR 向量"]
        M6["timestamp: INT64 时间戳"]
        M7["summary: VARCHAR 摘要"]
    end

    subgraph 股票数据集合["stock_data"]
        S1["id: INT64 主键"]
        S2["stock_code: VARCHAR 股票代码"]
        S3["stock_name: VARCHAR 股票名称"]
        S4["text: VARCHAR 文本"]
        S5["dense_vector: FLOAT_VECTOR 向量"]
        S6["sector: VARCHAR 行业"]
        S7["latest_price: DOUBLE 最新价"]
        S8["change_pct: DOUBLE 涨跌幅"]
    end
```

### 6.3 Redis数据结构

```mermaid
graph LR
    subgraph 短期记忆["chat:{user_id}:{role_id}"]
        H1["最近N条对话历史"]
        H2["格式: JSON数组"]
        H3["TTL: 1小时"]
    end

    subgraph 回答缓存["answer_cache"]
        C1["缓存Key: hash(user_id+role_id+query)"]
        C2["缓存Value: 完整回答"]
        C3["TTL: 300秒"]
    end

    subgraph 检索缓存["hybrid_search_cache"]
        S1["缓存Key: hash(role_id+query)"]
        S2["缓存Value: 检索结果"]
        S3["TTL: 300秒"]
    end
```

---

## 7. RAG核心能力

### 7.1 混合检索流程

```mermaid
flowchart TD
    A[用户Query] --> B[BGE-m3 向量化]
    B --> C[稠密向量查询]
    B --> D[BM25 稀疏向量]

    C --> E[Milvus IVF_FLAT检索]
    D --> F[Milvus SPARSE_INVERTED_INDEX检索]

    E --> G[Top-K 稠密结果]
    F --> H[Top-K 稀疏结果]

    G --> I[RRF融合]
    H --> I

    I --> J[BGE-Reranker重排序]
    J --> K[最终Top-K结果]

    K --> L[构建上下文]
    L --> M[发送给LLM]
```

### 7.2 Query改写机制

```mermaid
flowchart LR
    A[用户原始Query] --> B{历史记录>0?}
    B -->|是| C[基于历史改写]
    B -->|否| D[直接使用原始Query]

    C --> E[分析历史上下文]
    E --> F[补充缺失指代]
    F --> G[扩展同义词]

    D --> H[返回处理后Query]
    G --> H
```

---

## 8. 角色系统

### 8.1 角色列表

| ID | 角色名 | 显示名称 | 职责 |
|----|--------|----------|------|
| 1 | doctor | 医生 | 健康咨询 |
| 2 | financial_advisor | 金融理财师 | 理财建议 |
| 3 | investment_advisor | 投资顾问 | 投资分析 |
| 4 | financial_planner | 财务规划师 | 财务规划 |
| 5 | psychologist | 心理医生 | 心理咨询 |
| 6 | virtual_friend | 虚拟朋友 | 日常陪伴 |
| 7 | teacher | 教师 | 教育辅导 |
| 8 | lawyer | 律师 | 法律咨询 |
| 9 | scientist | 科学家 | 科学知识 |
| 10 | english_tutor | 英语助手 | 英语学习 |
| 11 | stock_analyst | 股票分析师 | 股票分析 |

### 8.2 角色提示词模板结构

```mermaid
graph TD
    A[ROLE_PROMPT_TEMPLATES] --> B["financial_advisor"]
    A --> C["doctor"]
    A --> D["investment_advisor"]
    A --> E["psychologist"]
    A --> ...["其他角色"]

    B --> B1[角色定义]
    B --> B2[回答要求]
    B --> B3[禁止事项]
    B --> B4[输出格式]
```

---

## 9. API接口详解

### 9.1 认证接口

```mermaid
sequenceDiagram
    participant Client as 前端
    participant API as api.py
    participant DB as database.py

    Note over Client,API: 用户注册
    Client->>API: POST /register {username, password, email, phone}
    API->>DB: register_user()
    DB->>DB: 检查用户名唯一性
    DB->>DB: 密码哈希加密
    DB->>DB: 插入users表
    DB-->>API: 注册成功
    API-->>Client: 返回用户信息

    Note over Client,API: 用户登录
    Client->>API: POST /login {username, password}
    API->>DB: login_user()
    DB->>DB: 密码验证
    DB-->>API: 返回user_id
    API-->>Client: 返回完整用户信息
```

### 9.2 对话接口（核心）

```mermaid
sequenceDiagram
    participant User as 用户
    participant Front as 前端
    participant API as api.py
    participant DB as database.py
    participant Utils as utils.py
    participant LLM as DeepSeek

    User->>Front: 发送消息 + 可选文件
    Front->>API: POST /api/chat_stream {user_id, role_id, message, file?}

    rect rgb(200, 230, 200)
        Note over API,LLM: 并行检索阶段
        API->>DB: retrieve_long_term_memory() [异步]
        API->>DB: retrieve_sqlite_role_knowledge() [异步]
        API->>DB: hybrid_search() [异步]
        API->>DB: retrieve_stock_data() [异步]
        DB-->>API: 四路检索结果
    end

    API->>API: rerank_documents() 重排序
    API->>API: build_llm_messages() 构建上下文

    rect rgb(200, 220, 255)
        Note over API,LLM: 流式生成阶段
        API->>LLM: chat.completions.create(stream=True)
        LLM-->>API: 流式tokens
        API-->>Front: SSE流式传输
        Front-->>User: 实时显示回答
    end

    API->>DB: store_long_term_memory() 保存记忆
    API->>DB: add_to_history() 更新短期记忆
```

---

## 10. 错误处理与降级机制

### 10.1 服务降级策略

```mermaid
flowchart TD
    A[请求到达] --> B{Milvus可用?}
    B -->|否| C[使用本地缓存降级]
    B -->|是| D{BM25可用?}

    D -->|否| E[仅稠密向量检索]
    D -->|是| F[完整混合检索]

    C --> G[Redis可用?]
    C -->|否| H[使用内存缓存]
    G -->|否| H

    F --> I{Reranker可用?}
    E --> I
    I -->|否| J[跳过重排序]
    I -->|是| K[执行重排序]

    J --> L[构建上下文]
    K --> L

    H --> L
    L --> M{LLM可用?}
    M -->|否| N[使用build_database_grounded_answer兜底]
    M -->|是| O[调用LLM]

    N --> P[返回兜底回答]
    O --> P
```

### 10.2 超时处理

| 操作 | 超时时间 | 降级策略 |
|------|----------|----------|
| 并行检索 | 45秒 | 超时任务返回空结果 |
| LLM调用 | 120秒 | 返回超时提示 |
| 文件解析 | 30秒 | 返回解析失败提示 |
| 数据库连接 | 30秒 | 抛出异常 |

---

## 11. 性能优化

### 11.1 缓存策略

```mermaid
graph LR
    subgraph 缓存层级
        L1["回答缓存 _answer_cache<br/>TTL: 300秒<br/>大小: 200条"]
        L2["检索缓存 hybrid_search_cache<br/>TTL: 300秒<br/>大小: 无限制"]
        L3["模型缓存 模型单例<br/>进程生命周期"]
    end

    subgraph 缓存命中
        H1["Query哈希]
        H2["context哈希]
        H1 --> L1
    end
```

### 11.2 并行处理

| 操作 | 并行方式 | 线程数 |
|------|----------|--------|
| 四路检索 | ThreadPoolExecutor | 4 |
| 模型预热 | 启动时加载 | - |
| 向量化 | BGE-m3批处理 | - |

---

## 12. 部署架构

### 12.1 组件架构

```mermaid
graph TB
    subgraph 前端服务["前端服务"]
        F1["web_frontend_show.py<br/>端口: 8505"]
    end

    subgraph 后端服务["后端服务"]
        B1["main.py<br/>端口: 8000"]
    end

    subgraph 数据服务["数据服务"]
        M1["MySQL<br/>端口: 3306"]
        M2["Milvus<br/>端口: 19530"]
        R1["Redis<br/>端口: 6379"]
    end

    subgraph 外部服务["外部服务"]
        L1["DeepSeek API"]
        E1["东方财富API"]
    end

    F1 --> B1
    B1 --> M1
    B1 --> M2
    B1 --> R1
    B1 --> L1
    B1 --> E1
```

### 12.2 启动命令

```bash
# 1. 启动后端服务
cd last_duan
python main.py

# 2. 启动前端服务（新终端）
cd web_frontend
python web_frontend_show.py

# 3. 访问
# 前端: http://127.0.0.1:8505
# 后端API: http://127.0.0.1:8000
# API文档: http://127.0.0.1:8000/docs
```

---

## 13. 版本历史

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0.0 | 2024-01 | 初始版本，实现基础RAG对话功能 |
| 1.1.0 | 2026-05 | 前端代码拆分、多模态支持、并行检索优化 |

---

## 14. 附录

### 14.1 环境变量配置

```bash
# 大模型配置
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=your_api_key

# 模型路径
BGE_M3_PATH=D:\模型\bge_m3_model
BGE_RERANKER_PATH=D:\模型\bge-reranker-base

# MySQL配置
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=roleplay

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379

# Milvus配置
MILVUS_HOST=192.168.18.128
MILVUS_PORT=19530
```

### 14.2 依赖列表

```
fastapi>=0.104.0
uvicorn>=0.24.0
pymilvus>=2.6.0
pymysql>=1.1.0
redis>=5.0.0
openai>=1.0.0
sentence-transformers>=2.2.0
pytesseract>=0.3.10
Pillow>=10.0.0
easyocr>=1.7.0
python-docx>=1.0.0
PyPDF2>=3.0.0
```

---

**文档版本**: 1.0.0
**最后更新**: 2026-05-09
**基于代码版本**: last_duan/ (api.py, database.py, utils.py, main.py)
